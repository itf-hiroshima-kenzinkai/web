---
import '../styles/global.css'
const { title } = Astro.props
---

<div
  class="w-[80%] flex flex-col items-center justify-center bg-[#E93A3A] mt-20 md:mt-80 rounded-4xl position: relative"
>
  <p class="md : font-jaro justify-center text-white text-4xl md:tracking-wider my-4 md:my-8 md:text-7xl">{title}</p>

  <div class="flex flex-col md:flex-row w-full px-16 font-zenmaru md:gap-12 md:mb-12">
    <slot />
  </div>
</div>
